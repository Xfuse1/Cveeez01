declare module 'react-day-picker';

// Minimal ambient declaration so TypeScript accepts imports when @types are not installed.
// TODO: replace with proper types or install `@types/react-day-picker` if available.
